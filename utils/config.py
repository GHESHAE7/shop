class Config:
    def __init__(self, sandbox, merchant_id, access_token, base_url):
        self.sandbox = sandbox  # True or False
        self.merchant_id = merchant_id
        self.access_token = access_token
        self.base_url = base_url
